<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose handles theming; this is the Activity-level theme used before setContent. -->
    <style name="Theme.NcCollectives" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Activity-attached splash theme. AndroidX core-splashscreen handles the
         API <31 backport; on API 31+ the platform renders it natively. -->
    <style name="Theme.NcCollectives.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.NcCollectives</item>
    </style>
</resources>
