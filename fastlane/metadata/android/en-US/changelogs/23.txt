Collaborative (Nextcloud Text) editor robustness: links you tap inside the editor — mentions, file references, external URLs — now open outside the editor instead of breaking your editing session, and a session the server invalidates now reloads itself. Footnotes authored in Nextcloud Text ([^1] references and their definitions) now render natively in the read view as numbered superscripts with a Footnotes list, instead of showing raw markdown.
