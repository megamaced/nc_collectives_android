Fixes pages failing to open with "The server returned 404" on Nextcloud installs that live in a subdirectory, such as https://example.com/nextcloud. Collectives and page lists loaded, but every page body request had the subdirectory added twice. Page bodies, attachments and images now all load on subdirectory installs. Installs at the root of a domain were never affected and are unchanged.
